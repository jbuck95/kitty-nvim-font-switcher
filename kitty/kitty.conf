# Kitty base config
# Used as base for font_*.conf includes.
# Requires: listen_on unix:/tmp/kitty (for kitty @ load-config)

# BEGIN_KITTY_FONTS
font_family      family="Droid Sans Mono"
bold_font        auto
italic_font      auto
bold_italic_font auto
# END_KITTY_FONTS
